<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Electron/V8 needs JIT compilation (just-in-time code generation). -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- V8 allocates executable memory for JIT code; must be allowed. -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow DYLD_* environment variables (Electron uses them internally). -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Disable library validation so unsigned native modules can load. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Allow loading of third-party libraries. -->
    <key>com.apple.security.cs.disable-executable-page-protection</key>
    <true/>
</dict>
</plist>